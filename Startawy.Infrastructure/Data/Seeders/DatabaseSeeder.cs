namespace startawy.Infrastructure.Data.Seeders;

// Legacy Identity-based seeder not used in the current setup.
// Kept as an empty placeholder to avoid pulling Identity into Infrastructure.
public static class DatabaseSeeder
{
}
